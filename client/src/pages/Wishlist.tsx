import PublicFooter from "@/components/PublicFooter";
import PublicHeader from "@/components/PublicHeader";
import PropertyCard from "@/components/PropertyCard";
import { useFavorites } from "@/hooks/useFavorites";
import { useRecentlyViewed } from "@/hooks/useRecentlyViewed";
import { formatNpr, getComparisonDifferenceKeys, Property, sortProperties, toggleComparisonId } from "@/lib/property";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, Check, ClipboardCheck, Clock3, Heart, Mail, Scale, Share2, Trash2, X } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { Link } from "wouter";

type WishlistSort = "newest" | "oldest" | "price-low" | "price-high";
const priceFilters = [
  { label: "Any price", value: "all" },
  { label: "Under Rs. 1 crore", value: "10000000" },
  { label: "Under Rs. 3 crore", value: "30000000" },
  { label: "Above Rs. 3 crore", value: "above" },
] as const;

export default function Wishlist() {
  const { favoriteIds, clearFavorites } = useFavorites();
  const { recentlyViewedEntries, clearRecentlyViewed } = useRecentlyViewed();
  const propertiesQuery = trpc.properties.list.useQuery();
  const [sort, setSort] = useState<WishlistSort>("newest");
  const [priceFilter, setPriceFilter] = useState<(typeof priceFilters)[number]["value"]>("all");
  const [compareIds, setCompareIds] = useState<string[]>(() => { const params = new URLSearchParams(window.location.search); const fromUrl = params.get("compare")?.split(",").filter(Boolean) ?? []; if (fromUrl.length) return fromUrl.slice(0, 3); try { return JSON.parse(localStorage.getItem("propnexus_compare_ids") ?? "[]").slice(0, 3); } catch { return []; } });
  const [shareStatus, setShareStatus] = useState<"idle" | "copied">("idle");
  const [emailRecipient, setEmailRecipient] = useState("");
  const [personalMessage, setPersonalMessage] = useState("");
  const emailComparison = trpc.comparison.emailPdf.useMutation();
  const [sharedIds] = useState<string[]>(() => new URLSearchParams(window.location.search).get("wishlist")?.split(",").map(id => id.trim()).filter(Boolean) ?? []);
  const isReadOnlyShared = sharedIds.length > 0;

  async function shareWishlist() {
    if (!favoriteIds.length) return;
    const url = `${window.location.origin}/wishlist?wishlist=${encodeURIComponent(favoriteIds.join(","))}`;
    try { await navigator.clipboard.writeText(url); } catch { window.prompt("Copy your PropNexus wishlist link", url); }
    setShareStatus("copied");
    window.setTimeout(() => setShareStatus("idle"), 2200);
  }

  const visibleFavoriteIds = isReadOnlyShared ? sharedIds : favoriteIds;
  const allProperties = (propertiesQuery.data ?? []) as Property[];
  const savedProperties = allProperties.filter(property => visibleFavoriteIds.includes(property.id));
  const recentProperties = recentlyViewedEntries.map(entry => ({ property: allProperties.find(property => property.id === entry.id), viewedAt: entry.viewedAt })).filter((entry): entry is { property: Property; viewedAt: number } => Boolean(entry.property)).filter(entry => !visibleFavoriteIds.includes(entry.property.id));
  function sendComparisonEmail(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault();
    emailComparison.mutate({ recipient: emailRecipient, personalMessage: personalMessage.trim() || undefined, properties: compareProperties.map(property => ({ title: property.title, location: property.location, city: property.city, price: formatNpr(property.price, property.listingType), propertyType: property.propertyType, areaSize: property.areaSize, bedrooms: property.bedrooms, status: property.status })) }, { onSuccess: () => { setEmailRecipient(""); setPersonalMessage(""); } });
  }
  const properties = useMemo(() => {
    const filtered = savedProperties.filter(property => {
      if (priceFilter === "all") return true;
      if (priceFilter === "above") return property.price > 30000000;
      return property.price <= Number(priceFilter);
    });
    if (sort === "oldest") return [...filtered].sort((left, right) => new Date(left.createdAt).getTime() - new Date(right.createdAt).getTime());
    return sortProperties(filtered, sort === "newest" ? "newest" : sort);
  }, [savedProperties, priceFilter, sort]);
  const compareProperties = compareIds.map(id => allProperties.find(property => property.id === id)).filter((property): property is Property => Boolean(property));
  const differenceKeys = useMemo(() => getComparisonDifferenceKeys(compareProperties), [compareProperties]);
  const formatViewedAt = (timestamp: number) => new Date(timestamp).toLocaleString("en-NP", { dateStyle: "medium", timeStyle: "short" });

  function toggleCompare(propertyId: string) {
    setCompareIds(current => { const next = toggleComparisonId(current, propertyId); localStorage.setItem("propnexus_compare_ids", JSON.stringify(next)); return next; });
  }

  return (
    <div className="min-h-screen bg-[#f8f5ee] text-[#10243a]"><PublicHeader />
      <main id="main-content">
        <section className="bg-[#0a1b2f] py-16 text-white sm:py-20"><div className="container"><p className="eyebrow text-[#d7b16c]">Your private shortlist</p><h1 className="mt-4 max-w-3xl font-display text-5xl leading-[.94] sm:text-6xl">A place for the properties <em className="font-light text-[#e5c37d]">worth returning to.</em></h1><p className="mt-6 max-w-xl text-sm leading-7 text-white/65">Organize saved properties by price, revisit the newest opportunities, and compare the ones that stand out.</p></div></section>
        <section className="container py-10 sm:py-14">
          <div className="mb-8 flex flex-col justify-between gap-5 lg:flex-row lg:items-center"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-[#e9ddc4] text-[#8e6b32]"><Heart className="h-4 w-4 fill-current" /></span><p className="text-sm text-slate-500"><span className="font-semibold text-[#10243a]">{properties.length}</span> of {savedProperties.length} saved {savedProperties.length === 1 ? "property" : "properties"}</p></div><div className="flex flex-wrap items-center gap-3"><label className="sr-only" htmlFor="wishlist-price">Filter wishlist by price</label><select id="wishlist-price" value={priceFilter} onChange={event => setPriceFilter(event.target.value as typeof priceFilter)} className="h-10 rounded-xl border border-[#10243a]/15 bg-white px-3 text-xs font-semibold outline-none focus:border-[#b78c43]">{priceFilters.map(option => <option key={option.value} value={option.value}>{option.label}</option>)}</select><label className="sr-only" htmlFor="wishlist-sort">Sort wishlist</label><select id="wishlist-sort" value={sort} onChange={event => setSort(event.target.value as WishlistSort)} className="h-10 rounded-xl border border-[#10243a]/15 bg-white px-3 text-xs font-semibold outline-none focus:border-[#b78c43]"><option value="newest">Newest saved listings</option><option value="oldest">Oldest listings</option><option value="price-low">Price: low to high</option><option value="price-high">Price: high to low</option></select>{!isReadOnlyShared && favoriteIds.length > 0 && <><button onClick={shareWishlist} className="inline-flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.12em] text-[#8e6b32] transition hover:text-[#10243a]">{shareStatus === "copied" ? <ClipboardCheck className="h-3.5 w-3.5" /> : <Share2 className="h-3.5 w-3.5" />} {shareStatus === "copied" ? "Link copied" : "Share wishlist"}</button><button onClick={clearFavorites} className="inline-flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.12em] text-rose-700 transition hover:text-rose-900"><Trash2 className="h-3.5 w-3.5" /> Clear wishlist</button></>}</div></div>
          {!isReadOnlyShared && recentlyViewedEntries.length > 0 && <section aria-labelledby="recently-viewed-title" className="mb-10"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2"><Clock3 className="h-4 w-4 text-[#a67e35]" /><h2 id="recently-viewed-title" className="font-display text-2xl">Recently viewed</h2></div><button onClick={clearRecentlyViewed} className="text-[10px] font-bold uppercase tracking-[.12em] text-rose-700 transition hover:text-rose-900">Clear history</button></div><p className="mb-5 text-sm text-slate-500">Properties you looked at recently, in case one slipped past your saved list.</p><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{recentProperties.map(({ property, viewedAt }) => <div key={property.id}><PropertyCard property={property} compact /><p className="mt-2 text-[11px] text-slate-500">Viewed {formatViewedAt(viewedAt)}</p></div>)}</div></section>}
          {compareProperties.length > 0 && <section id="comparison-print-area" aria-labelledby="compare-title" className="mb-10 overflow-hidden rounded-2xl border border-[#d7b16c]/40 bg-[#0b1c2f] text-white shadow-[0_18px_55px_rgba(8,23,41,.14)]"><div className="print-only mb-8 items-center justify-between border-b border-[#d7b16c] pb-5"><div className="flex items-center gap-3"><img src="/manus-storage/propnexus-logo-clean_2e81583e.png" alt="PropNexus" className="h-12 w-auto" /><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#8e6b32]">Property comparison brief</p><p className="mt-1 text-xs text-slate-500">Prepared {new Date().toLocaleDateString("en-NP")}</p></div></div><p className="text-right text-xs font-semibold text-[#10243a]">Curated Nepal property shortlist</p></div><div className="flex flex-col justify-between gap-4 border-b border-white/10 p-5 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><Scale className="h-5 w-5 text-[#e5c37d]" /><div><h2 id="compare-title" className="font-display text-2xl">Compare saved properties</h2><p className="mt-1 text-xs text-white/55">{compareProperties.length} of 3 comparison slots used.</p></div></div><div className="flex flex-wrap items-center gap-4"><button onClick={() => window.print()} className="inline-flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.12em] text-[#e5c37d] hover:text-white"><ClipboardCheck className="h-3.5 w-3.5" /> Export comparison PDF</button><form onSubmit={sendComparisonEmail} className="flex flex-wrap items-center gap-2"><label className="sr-only" htmlFor="comparison-email">Email comparison PDF to</label><input id="comparison-email" type="email" required value={emailRecipient} onChange={event => setEmailRecipient(event.target.value)} placeholder="recipient@email.com" className="h-8 w-48 rounded-full border border-white/15 bg-white/10 px-3 text-[11px] text-white placeholder:text-white/40 outline-none focus:border-[#d7b16c]" /><label className="sr-only" htmlFor="comparison-message">Personal message</label><input id="comparison-message" value={personalMessage} onChange={event => setPersonalMessage(event.target.value)} maxLength={1000} placeholder="Add a personal note (optional)" className="h-8 w-56 rounded-full border border-white/15 bg-white/10 px-3 text-[11px] text-white placeholder:text-white/40 outline-none focus:border-[#d7b16c]" /><button type="submit" disabled={emailComparison.isPending} className="inline-flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.12em] text-[#e5c37d] hover:text-white disabled:opacity-50"><Mail className="h-3.5 w-3.5" /> {emailComparison.isPending ? "Sending…" : "Email PDF"}</button></form><button onClick={() => setCompareIds([])} className="inline-flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.12em] text-[#e5c37d] hover:text-white"><X className="h-3.5 w-3.5" /> Clear comparison</button></div></div><div className="overflow-x-auto"><div className="grid min-w-[720px] grid-cols-3 divide-x divide-white/10">{compareProperties.map(property => <article key={property.id} className="p-5"><div className="relative overflow-hidden rounded-xl"><img src={property.featuredImage} alt={property.title} className="aspect-[4/3] w-full object-cover" /><button onClick={() => toggleCompare(property.id)} aria-label={`Remove ${property.title} from comparison`} className="absolute right-2 top-2 grid h-8 w-8 place-items-center rounded-full bg-[#081729]/80 text-white"><X className="h-3.5 w-3.5" /></button></div><h3 className="mt-4 font-display text-xl">{property.title}</h3><p className="mt-1 text-xs text-white/55">{property.location}, {property.city}</p><dl className="mt-5 grid gap-3 text-xs"><div className={`flex justify-between gap-3 rounded-lg ${differenceKeys.has("price") ? "bg-[#d7b16c]/10 px-2 py-1.5 ring-1 ring-[#d7b16c]/35" : ""}`}><dt className="text-white/50">Price</dt><dd className="flex items-center gap-2 text-right font-semibold text-[#e5c37d]">{formatNpr(property.price, property.listingType)}{differenceKeys.has("price") && <span className="rounded-full bg-[#d7b16c] px-1.5 py-0.5 text-[8px] font-bold uppercase tracking-wide text-[#10243a]">Varies</span>}</dd></div><div className={`flex justify-between gap-3 rounded-lg ${differenceKeys.has("type") ? "bg-[#d7b16c]/10 px-2 py-1.5 ring-1 ring-[#d7b16c]/35" : ""}`}><dt className="text-white/50">Type</dt><dd>{property.propertyType}{differenceKeys.has("type") && <span className="ml-2 rounded-full bg-[#d7b16c] px-1.5 py-0.5 text-[8px] font-bold uppercase tracking-wide text-[#10243a]">Varies</span>}</dd></div><div className={`flex justify-between gap-3 rounded-lg ${differenceKeys.has("area") ? "bg-[#d7b16c]/10 px-2 py-1.5 ring-1 ring-[#d7b16c]/35" : ""}`}><dt className="text-white/50">Area</dt><dd>{property.areaSize}{differenceKeys.has("area") && <span className="ml-2 rounded-full bg-[#d7b16c] px-1.5 py-0.5 text-[8px] font-bold uppercase tracking-wide text-[#10243a]">Varies</span>}</dd></div><div className={`flex justify-between gap-3 rounded-lg ${differenceKeys.has("bedrooms") ? "bg-[#d7b16c]/10 px-2 py-1.5 ring-1 ring-[#d7b16c]/35" : ""}`}><dt className="text-white/50">Bedrooms</dt><dd>{property.bedrooms || "—"}{differenceKeys.has("bedrooms") && <span className="ml-2 rounded-full bg-[#d7b16c] px-1.5 py-0.5 text-[8px] font-bold uppercase tracking-wide text-[#10243a]">Varies</span>}</dd></div><div className={`flex justify-between gap-3 rounded-lg ${differenceKeys.has("status") ? "bg-[#d7b16c]/10 px-2 py-1.5 ring-1 ring-[#d7b16c]/35" : ""}`}><dt className="text-white/50">Status</dt><dd>{property.status}{differenceKeys.has("status") && <span className="ml-2 rounded-full bg-[#d7b16c] px-1.5 py-0.5 text-[8px] font-bold uppercase tracking-wide text-[#10243a]">Varies</span>}</dd></div></dl><Link href={`/properties/${property.slug}`} className="mt-5 inline-flex text-[10px] font-bold uppercase tracking-[.12em] text-[#e5c37d]">View details</Link></article>)}</div></div>{emailComparison.isSuccess && <p className="px-5 pb-3 text-xs text-emerald-300">Comparison PDF emailed successfully.</p>}{emailComparison.isError && <p className="px-5 pb-3 text-xs text-rose-300">{emailComparison.error.message}</p>}<div className="print-only mt-8 border-t border-[#d7b16c] pt-4 text-xs text-slate-600"><strong className="text-[#10243a]">PropNexus</strong> · Contact Abhay at +977 9769279600 or rawalabhaya!@gmail.com for current availability and viewings.</div></section>}
          {isReadOnlyShared && <div className="mb-6 rounded-2xl border border-[#d7b16c]/45 bg-[#f7efdd] px-5 py-4 text-[#7d5d25]"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.12em]"><Scale className="h-4 w-4" /> Shared wishlist · read-only viewing</div><p className="mt-2 max-w-2xl text-sm leading-6">You’re viewing a curated list of properties shared through PropNexus. Browse the details and compare the homes selected for you; your saved properties remain unchanged.</p></div>}{propertiesQuery.isLoading ? <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{Array.from({ length: 3 }, (_, index) => <div key={index} className="h-[400px] animate-pulse rounded-[1.35rem] bg-[#e9e4d8]" />)}</div> : propertiesQuery.isError ? <div role="alert" className="rounded-2xl border border-rose-200 bg-rose-50 px-6 py-16 text-center"><p className="font-display text-3xl">Your wishlist is temporarily unavailable.</p><button onClick={() => propertiesQuery.refetch()} className="mt-6 rounded-full bg-[#10243a] px-5 py-2.5 text-xs font-bold text-white">Try again</button></div> : properties.length ? <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{properties.map(property => <article key={property.id} className="relative"><button onClick={() => toggleCompare(property.id)} aria-pressed={compareIds.includes(property.id)} className={`absolute right-3 top-3 z-10 inline-flex items-center gap-1.5 rounded-full px-3 py-2 text-[10px] font-bold uppercase tracking-[.08em] shadow-md transition ${compareIds.includes(property.id) ? "bg-[#d7b16c] text-[#10243a]" : "bg-[#081729]/85 text-white hover:bg-[#d7b16c] hover:text-[#10243a]"}`}><Check className={`h-3 w-3 ${compareIds.includes(property.id) ? "opacity-100" : "opacity-0"}`} /> Compare</button><PropertyCard property={property} compact readOnly={isReadOnlyShared} /></article>)}</div> : <div className="rounded-2xl border border-dashed border-[#10243a]/20 bg-white px-6 py-20 text-center"><Heart className="mx-auto h-8 w-8 text-[#b78c43]" /><p className="mt-5 font-display text-3xl">{savedProperties.length ? "No saved properties match this price filter." : "Your wishlist is waiting."}</p><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-500">{savedProperties.length ? "Try another price range to bring a saved property back into view." : "Save a property from the catalogue or a detail page and it will appear here for later viewing."}</p><Link href="/properties" className="mt-6 inline-flex items-center gap-2 rounded-full bg-[#10243a] px-5 py-3 text-xs font-bold text-white"><ArrowLeft className="h-4 w-4" /> Explore properties</Link></div>}
        </section>
      </main><PublicFooter />
    </div>
  );
}
